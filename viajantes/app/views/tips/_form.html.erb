<% form_for([:tourist_sight, @tip]) do |f| %>
	<%= f.error_messages %>

	<p>
		Nome:<br />
		<%= f.text_field :name %>
	</p>
	<p>
		Descrição: <br />
		<%= f.text_area :description %>
	</p>
	<p>
		<%= f.submit 'Salvar' %>
	</p>
<% end %>
